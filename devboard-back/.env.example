# Application
NODE_ENV=development
PORT=3000

# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/devboard?schema=public"

# CORS — comma-separated allowed origins
CORS_ORIGINS=http://localhost:5173

# Rate limiting (ms / requests per TTL window)
THROTTLE_TTL=60000
THROTTLE_LIMIT=100
