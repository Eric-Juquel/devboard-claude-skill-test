# Test environment — uses a separate database to keep dev data untouched
NODE_ENV=test
PORT=3001
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/devboard_test?schema=public"
CORS_ORIGINS=http://localhost:5173
THROTTLE_TTL=60000
THROTTLE_LIMIT=1000
